<p class="mb-2 text-sm text-gray-600">
  This shared folder link will expire after the selected date.
</p>
<div class="relative flex items-center gap-2">
  <input
    id="shareLink"
    type="text"
    value="<%= createdSharedFolderLink %>"
    readonly
    class="w-full rounded-md border border-gray-300 px-4 py-2 text-sm text-gray-900 focus:outline-none focus:ring-2 focus:ring-blue-500"
  />

  <button
    autofocus
    onclick="
        const input = document.getElementById('shareLink');
        input.select();
        input.setSelectionRange(0, 99999);

        navigator.clipboard.writeText(input.value)
        .then(() => {
            event.target.textContent = 'Copied!'
        })
        .catch((err) => {
            console.error('Failed to copy:', err);
        });
        "
    type="button"
    class="flex items-center gap-1 rounded-md bg-blue-600 px-3 py-2 text-sm text-white hover:bg-blue-500 transition"
  >
    <i class="fa fa-copy"></i>
    Copy
  </button>
</div>
